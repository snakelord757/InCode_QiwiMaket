<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="100dp"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_marginEnd="5dp">


    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/payment_item_icon"
        android:layout_width="@dimen/circle_size"
        android:layout_height="@dimen/circle_size"
        android:src="@drawable/ic_payments_icon"
        app:civ_border_width="2dp"
        app:civ_border_color="@android:color/darker_gray"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!--<FrameLayout
        android:id="@+id/circle_background"
        android:layout_width="@dimen/circle_size"
        android:layout_height="@dimen/circle_size"
        android:background="@drawable/payments_and_transaction_item_background"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <ImageView
        android:id="@+id/payment_item_icon"
        android:src="@drawable/ic_android_gray_24dp"
        android:tint="@color/colorPrimary"
        android:layout_width="@dimen/payments_icon_size"
        android:layout_height="@dimen/payments_icon_size"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/circle_background"/>-->

    <TextView
        android:id="@+id/payment_item_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/payments_title_size"
        android:textColor="@android:color/black"
        android:layout_marginTop="@dimen/top_margin_10"
        android:gravity="center_horizontal"
        android:maxEms="6"
        app:layout_constraintTop_toBottomOf="@id/payment_item_icon"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="Название услуги"/>

</androidx.constraintlayout.widget.ConstraintLayout>